# Exercício

## 1. Na aplicação utilizada em aula, fazer o que se pede:

- após a autenticação do usuário, definir um email para ele (implementar no app.component)

- passar esse dado para o componente de contato e com isso, o campo email do formulário carregar o email do usuário autenticado.

- exibir a seção de endereço somente quando houver um usuário autenticado

- Verificar os métodos do ciclo de vida (ngOnInit, ngOnChanges, ngOnDestroy) para cada etapa desse fluxo
